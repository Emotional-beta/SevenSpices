# 《七荤八素》界面风格规范

> 版本：v1
> 文档状态：已定稿
> 定位：美术真源（**界面 / 局内玩法层**）。所有 UI、局内画面、图标与特效统一遵循本文档。
> 关系：人物立绘层（#191 淡彩）见《立绘风格规范》；叙事见《故事与世界观设计文档》。

---

# 一、风格定义

- **统一风格**：Spellbook **#069 像素**（16-bit 像素表现）。
- **范围**：**全像素** —— 界面、局内场景、局内出现的角色、食材与道具图标、特效、装饰，全部为像素。
- **视觉特征**：清晰可见的像素栅格、纯色块 + 1px 描边、温暖调、亲和。
- **贯穿母题**：**青铜饕餮纹（兽面纹）**，以点阵纹样形式出现在面板与边框上。

> 依据：2026-09 风格横比（10 种风格）后选定 #069；`ui-01`（淡彩立绘嵌入像素面板）与 `ui-01b`（全像素）对照后，**确认采用全像素**，界面内不嵌入立绘。

---

# 二、与立绘层的关系

| 层 | 用途 | 风格 |
|---|---|---|
| **界面 / 局内层**（本文档） | 全部 UI、局内角色、锅、图标、特效 | **#069 像素（全像素）** |
| **立绘层**（见《立绘风格规范》） | 剧情插图、演出大图、卡面插画、宣发 | **#191 宋画淡彩** |

- 两层共用**青铜饕餮纹**母题与**同一套调色板**。
- **叙事屏使用立绘大图**：章末验收 / 开局 / 结局等叙事屏，使用 #191 淡彩立绘**大图**作为主视觉，叠加在像素界面之上。

---

# 三、配色

- **界面基色**：墨黑、青铜绿 / 氧化铜、暗金、赭石、石青
- **唯一强调色**：**朱砂红**（告警、超频、爆点）
- **汤面**：**统一淡金黄（鸡汤色）**，不做七层彩汤
- **七味标识色**（仅用于 HUD 与图标，不落在汤面）：
  酸=青柠绿、甜=琥珀金、苦=深褐橄榄、辣=朱砂红、鲜=乳白象牙、咸=月白银白、麻=青紫罗兰
- 目标：低饱和、克制、有"出土青铜器"的斑驳感。

---

# 四、栅格与缩放

- **窗口基准**：1280×720。
- **最小单位**：**8 的倍数**（所有 UI 尺寸、间距、图标与面板边角都对齐到 8px 栅格）。
- **缩放**：像素资源按 **2× / 3× 整数倍**缩放。
- **Godot 设置**：`Filter = Nearest`、关闭 Mipmap、开启整数缩放与 pixel snap。
- **描边**：1px 深色描边，保证在暗底与亮底上都清晰。

---

# 五、字体

- **正文：易读字体**（常规中文无衬线 / 宋体），保证长文本与说明的可读性。
- **标题与数字：像素字体**——画面标题、分数、倍率、金币等短文本与数值用像素字体。
- **不用像素字体覆盖正文**。
- 字号必须落在像素栅格上（配合 8 倍数单位）。
- 待办：易读正文字体与像素字体的具体选型、授权与字形覆盖核实。

---

# 六、信息层级（常驻精简 + 按需展开）

- **常驻**：分数、倍率、七味条。
- **按需展开**（悬停 / 抽屉 / tooltip）：陈酿池、固化、物理状态「臭」、味道种类数、味道熵倍率、锅底等。
- 原则：默认界面只回答"我现在多少分、还剩什么机会"；细节按需拉出。

---

# 七、叙事层题材包装：饕餮的食谱残卷

- 叙事与提示统一包装成**女饕餮边啃边批注的上古食谱残卷**。
- 她的台词、毒舌批注、玩法提示都落在这本残卷上。
- 用途：开局、章末验收、结局、教程与提示。

---

# 八、界面清单（已有像素效果图）

| 界面 | 效果图 |
|---|---|
| 局内烹饪 HUD | `generated-images/界面-像素成套/ui-01b-hud-allpixel.png` |
| 锅结束奖励（X 选 1 + 伙伴候选） | `generated-images/界面-像素成套/ui-02-reward.png` |
| 商店 | `generated-images/界面-像素成套/ui-03-shop.png` |
| 章末饕餮验收 | `generated-images/界面-像素成套/ui-04-taotie-check.png` |
| 路线选择（监味星君） | `generated-images/界面-像素成套/ui-05-route.png` |

---

# 九、生图技术约定

1. **模型**：调用 `generate_image` 时必须**显式指定** `model = "[次]gpt-image-2.5"`。
2. **尺寸**：界面用 `1536x1024`；`quality = high`；`n = 1`。
3. **文件名**：kebab-case 英文（如 `ui-01b-hud-allpixel.png`）。
4. **存放**：`generated-images/界面-像素成套/`。
5. **版本管理**：`generated-images/` 已加入 `.gitignore`，**图片不入版本库**。

---

# 十、已知约束与待办

1. **AI 出图不能直接产出可切片的九宫格边框**（边缘不规整、不对称、不可平铺）。AI 图只作风格参考与整屏效果图；**实际 UI 边框 / 按钮 / 卡片需人工在 Aseprite 重绘**。
2. **待办**：易读正文字体与像素字体的选型、授权与字形覆盖核实。
3. **待办**：UI 控件清单与 8px 栅格规格落地到 Godot。
